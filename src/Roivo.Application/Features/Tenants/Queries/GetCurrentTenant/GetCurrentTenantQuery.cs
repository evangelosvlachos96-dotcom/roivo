namespace Roivo.Application.Features.Tenants.Queries.GetCurrentTenant;

/// <summary>Loads the caller's own tenant row (resolved via <c>ITenantContext</c>).</summary>
public sealed record GetCurrentTenantQuery;
